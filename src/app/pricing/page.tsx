import { PricingClient } from './PricingClient';

export const metadata = {
  title: 'Pricing — VaultFill',
  description:
    'Enterprise-Grade Security. Monthly Flexibility. Choose a plan to unlock your Autonomous Audit.',
};

export default function PricingPage() {
  return <PricingClient />;
}
